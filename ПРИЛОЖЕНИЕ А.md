ПРИЛОЖЕНИЕ А
(обязательное)
Листинг программы

---

// Файл nuxt.config.ts — конфигурация Nuxt
```ts
export default defineNuxtConfig({
  compatibilityDate: '2025-07-15',
  devtools: { enabled: false },
  devServer: {
    host: '0.0.0.0',
    port: 3000
  },
  ssr: true,
  app: {
    pageTransition: { name: 'page', mode: 'out-in' },
    head: {
      htmlAttrs: { lang: 'ru' },
      title: 'CLICKWOOD — Интерьерная мебель',
      meta: [
        { name: 'viewport', content: 'width=device-width, initial-scale=1' },
        { name: 'description', content: 'CLICKWOOD — стильная и надёжная мебель для вашего интерьера.' },
        { name: 'keywords', content: 'мебель, интерьер, диваны, кресла, столы, CLICKWOOD' }
      ],
      link: [{ rel: 'icon', type: 'image/x-icon', href: '/favicon.ico' }]
    }
  },
  modules: ['@nuxtjs/tailwindcss', '@nuxt/image'],
  runtimeConfig: {
    mongodbUri: process.env.MONGODB_URI,
    jwtSecret: process.env.JWT_SECRET
  }
})
```

---

// Файл tailwind.config.js — конфигурация Tailwind CSS
```js
export default {
  content: [
    "./components/**/*.{js,vue,ts}",
    "./layouts/**/*.vue",
    "./pages/**/*.vue",
    "./app.vue"
  ],
  theme: {
    container: { center: true, padding: { DEFAULT: '1rem', sm: '2rem', lg: '2rem', xl: '3rem', '2xl': '4rem' } },
    extend: {
      colors: {
        primary: '#788678',
        primaryDark: '#667566',
        textMain: '#523E33',
        textLight: '#FFFFFF',
        border: '#E5E5E5',
        brown: '#563F30'
      },
      fontFamily: {
        heading: ['Roscha', 'sans-serif'],
        body: ['"Arsenal SC"', 'sans-serif']
      }
    }
  },
  plugins: []
}
```

---

// Файл app.vue — корневой компонент приложения
```vue
<script setup>
useHead({
  bodyAttrs: { class: 'font-body text-textMain m-0 p-0' }
})
</script>

<template>
  <div class="min-h-screen flex flex-col">
    <NuxtLayout>
      <NuxtPage />
    </NuxtLayout>
  </div>
</template>

<style>
/* Page transitions */
.page-enter-active, .page-leave-active { transition: opacity 0.2s ease; }
.page-enter-from, .page-leave-to { opacity: 0; }

@font-face {
  font-family: 'Roscha';
  src: url('/fonts/ofont.ru_Roscha.ttf') format('truetype');
  font-display: swap;
}
@font-face {
  font-family: 'Arsenal SC';
  src: url('/fonts/ArsenalSC-Regular.ttf') format('truetype');
  font-weight: 400;
  font-display: swap;
}
@font-face {
  font-family: 'Arsenal SC';
  src: url('/fonts/ArsenalSC-Bold.ttf') format('truetype');
  font-weight: 700;
  font-display: swap;
}
* { box-sizing: border-box; }
html, body { overflow-x: hidden; width: 100%; }
</style>
```

---

// Файл layouts/default.vue — корневой макет
```vue
<template>
  <div class="flex flex-col min-h-screen">
    <Header />
    <main class="flex-1">
      <slot />
    </main>
    <Footer />
    <CartNotification />
  </div>
</template>

<script setup>
import Footer from '~/components/Footer.vue'
import CartNotification from '~/components/CartNotification.vue'
</script>
```

---

// Файл components/Header.vue — шапка сайта (с бургер-меню)
```vue
<script setup lang="ts">
import { ref, onMounted, onBeforeUnmount } from 'vue'
import { useCart } from '~/composables/useCart'
import { useCity, CITIES } from '~/composables/useCity'
import { searchProducts, type Product } from '~/composables/useProducts'

const isMobileMenuOpen = ref(false)
const { totalItems } = useCart()
const { selectedCity, isOpen, selectCity, toggleDropdown, closeDropdown } = useCity()

function toggleMobileMenu() { isMobileMenuOpen.value = !isMobileMenuOpen.value }
function closeMobileMenu() { isMobileMenuOpen.value = false }

// Search
const searchQuery = ref('')
const searchResults = ref<Product[]>([])
const isSearchOpen = ref(false)
const searchRef = ref<HTMLElement | null>(null)
let debounceTimer: ReturnType<typeof setTimeout> | null = null

async function handleSearchInput() {
  if (debounceTimer) clearTimeout(debounceTimer)
  debounceTimer = setTimeout(async () => {
    if (searchQuery.value.trim()) {
      searchResults.value = await searchProducts(searchQuery.value)
      isSearchOpen.value = searchResults.value.length > 0
    } else {
      searchResults.value = []
      isSearchOpen.value = false
    }
  }, 300)
}

// ... (полный код содержит обработчики поиска, клика вне дропдауна)
</script>

<template>
  <header class="w-full">
    <!-- Top bar с выбором города -->
    <div class="h-[50px] bg-primary text-brown">
      <div class="container mx-auto px-4 h-full flex items-center justify-between">
        <div ref="dropdownRef" class="relative">
          <button @click="toggleDropdown" class="flex items-center gap-1.5 hover:opacity-80">
            <svg><!-- иконка локации --></svg>
            {{ selectedCity.name }}
            <svg><!-- стрелка --></svg>
          </button>
          <!-- Выпадающий список городов -->
        </div>
        <button>Связаться с нами</button>
      </div>
    </div>

    <!-- Main header -->
    <div class="py-4 border-b border-primaryDark">
      <div class="container mx-auto px-4 flex items-center justify-between md:grid md:grid-cols-3">
        <!-- Бургер-кнопка (мобильная) -->
        <button @click="toggleMobileMenu" class="md:hidden">
          <svg><!-- три линии --></svg>
        </button>

        <!-- Навигация (десктоп) -->
        <nav class="hidden md:flex gap-8 md:justify-self-start">
          <NuxtLink to="/catalog">Каталог</NuxtLink>
          <NuxtLink to="/collections">Коллекции</NuxtLink>
          <NuxtLink to="/about">О нас</NuxtLink>
        </nav>

        <!-- Логотип -->
        <NuxtLink to="/" class="font-heading font-bold text-3xl text-brown md:justify-self-center">
          CLICKWOOD
        </NuxtLink>

        <!-- Правая секция: корзина + аккаунт (всегда видны) -->
        <div class="flex items-center gap-4 md:gap-6 md:justify-self-end">
          <NuxtLink to="/cart" @click="closeMobileMenu">
            <svg><!-- иконка корзины --></svg>
            <span v-if="totalItems > 0">{{ totalItems }}</span>
          </NuxtLink>
          <NuxtLink to="/account" @click="closeMobileMenu">
            <svg><!-- иконка профиля --></svg>
          </NuxtLink>

          <!-- Десктопные элементы (скрыты на мобильных) -->
          <div class="hidden md:flex items-center gap-6">
            <NuxtLink to="/delivery">Доставка</NuxtLink>
            <div ref="searchRef">
              <input v-model="searchQuery" placeholder="Поиск..." />
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Мобильное меню -->
    <Transition>
      <div v-if="isMobileMenuOpen" class="md:hidden bg-white border-b shadow-lg">
        <div class="container mx-auto px-4 py-4 flex flex-col gap-4">
          <NuxtLink @click="closeMobileMenu" to="/catalog">Каталог</NuxtLink>
          <NuxtLink @click="closeMobileMenu" to="/collections">Коллекции</NuxtLink>
          <NuxtLink @click="closeMobileMenu" to="/about">О нас</NuxtLink>
          <NuxtLink @click="closeMobileMenu" to="/delivery">Доставка</NuxtLink>
          <input v-model="searchQuery" placeholder="Поиск..." class="w-full" />
        </div>
      </div>
    </Transition>
  </header>
</template>
```

---

// Файл components/Footer.vue — подвал сайта
```vue
<template>
  <footer class="bg-primary text-gray-300">
    <div class="container mx-auto px-4 py-16">
      <div class="grid grid-cols-1 md:grid-cols-3 gap-12 md:gap-16">
        <!-- Колонка 1: бренд и контакты -->
        <div>
          <NuxtLink to="/" class="font-heading text-white text-4xl md:text-5xl">CLICKWOOD</NuxtLink>
          <div class="space-y-3 mt-6">
            <p>Москва, ул. Производственная, 12</p>
            <p><a href="tel:+74951234567">+7 (495) 123-45-67</a></p>
            <p><a href="mailto:info@clickwood.ru">info@clickwood.ru</a></p>
          </div>
          <!-- Социальные сети -->
          <div class="flex items-center gap-4 mt-8">
            <a href="#" aria-label="Instagram"><!-- SVG --></a>
            <a href="#" aria-label="VK"><!-- SVG --></a>
            <a href="#" aria-label="Telegram"><!-- SVG --></a>
            <a href="#" aria-label="WhatsApp"><!-- SVG --></a>
          </div>
        </div>
        <!-- Колонка 2: Навигация -->
        <div>
          <h4 class="text-white uppercase tracking-widest text-xs mb-6">Навигация</h4>
          <ul>
            <li><NuxtLink to="/">Главная</NuxtLink></li>
            <li><NuxtLink to="/catalog">Каталог</NuxtLink></li>
            <li><NuxtLink to="/collections">Коллекции</NuxtLink></li>
            <li><NuxtLink to="/about">О нас</NuxtLink></li>
            <li><NuxtLink to="/delivery">Доставка</NuxtLink></li>
          </ul>
        </div>
        <!-- Колонка 3: Покупателям -->
        <div>
          <h4 class="text-white uppercase tracking-widest text-xs mb-6">Покупателям</h4>
          <ul>
            <li><a href="/payment">Оплата</a></li>
            <li><a href="/delivery">Возврат</a></li>
            <li><a href="/warranty">Гарантия</a></li>
            <li><a href="/faq">Вопросы и ответы</a></li>
            <li><a href="/certificates">Сертификаты</a></li>
          </ul>
        </div>
      </div>
    </div>
    <!-- Нижняя полоса -->
    <div class="border-t border-gray-800">
      <div class="container mx-auto px-4 py-6 flex justify-between">
        <p>© {{ new Date().getFullYear() }} CLICKWOOD. Все права защищены.</p>
        <div class="flex gap-6">
          <a href="/privacy">Политика конфиденциальности</a>
          <a href="/terms">Пользовательское соглашение</a>
        </div>
      </div>
    </div>
  </footer>
</template>
```

---

// Файл components/CartNotification.vue — уведомления (успех/предупреждение)
```vue
<template>
  <Transition name="notification">
    <div v-if="notificationVisible" class="fixed top-6 right-6 z-[100] max-w-sm w-full">
      <div
        class="rounded-2xl shadow-xl p-5 flex items-start gap-4"
        :class="notificationType === 'warning'
          ? 'bg-amber-50 border border-amber-200'
          : 'bg-white border border-border'"
      >
        <div
          class="w-10 h-10 rounded-full flex items-center justify-center flex-shrink-0"
          :class="notificationType === 'warning' ? 'bg-amber-100' : 'bg-primary/10'"
        >
          <!-- Иконка предупреждения или успеха -->
          <svg v-if="notificationType === 'warning'" class="w-5 h-5 text-amber-600" viewBox="0 0 24 24" fill="none" stroke="currentColor">
            <path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/>
            <line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/>
          </svg>
          <svg v-else class="w-5 h-5 text-primary" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
            <polyline points="20 6 9 17 4 12" />
          </svg>
        </div>
        <div class="flex-1 min-w-0">
          <p :class="notificationType === 'warning' ? 'text-amber-800' : 'text-textMain'">
            {{ notificationText }}
          </p>
          <NuxtLink v-if="notificationType === 'success'" to="/cart">Перейти в корзину →</NuxtLink>
        </div>
        <button @click="notificationVisible = false">✕</button>
      </div>
    </div>
  </Transition>
</template>

<script setup lang="ts">
import { useCart } from '~/composables/useCart'
const { notificationText, notificationVisible, notificationType } = useCart()
</script>

<style scoped>
.notification-enter-active { transition: all 0.35s cubic-bezier(0.16, 1, 0.3, 1); }
.notification-leave-active { transition: all 0.2s ease-in; }
.notification-enter-from { opacity: 0; transform: translateX(60px) scale(0.95); }
.notification-leave-to { opacity: 0; transform: translateX(40px) scale(0.95); }
</style>
```

---

// Файл composables/useCity.ts — composable для выбора города
```ts
import { ref } from 'vue'

export interface City { name: string }

export const CITIES: City[] = [
  { name: 'Москва' }, { name: 'Санкт-Петербург' },
  { name: 'Новосибирск' }, { name: 'Екатеринбург' },
  { name: 'Казань' }, { name: 'Нижний Новгород' },
  { name: 'Челябинск' }, { name: 'Красноярск' },
  { name: 'Самара' }, { name: 'Уфа' }
]

export const DEFAULT_CITY: City = { name: 'Москва' }

const selectedCity = ref<City>(DEFAULT_CITY)
const isOpen = ref(false)

function findCity(name: string): City {
  return CITIES.find(c => c.name.toLowerCase() === name.toLowerCase()) ?? DEFAULT_CITY
}

function saveCity(city: City): void {
  if (process.client) localStorage.setItem('clickwood_city', JSON.stringify(city))
}

async function fetchCityByIP(): Promise<string | null> {
  try {
    const res = await fetch('https://ip-api.com/json')
    if (!res.ok) return null
    const data = await res.json()
    return data.city ?? null
  } catch { return null }
}

function loadCity(): void {
  if (process.client) {
    const saved = localStorage.getItem('clickwood_city')
    if (saved) {
      try {
        const parsed = JSON.parse(saved)
        if (parsed?.name) { selectedCity.value = findCity(parsed.name); return }
      } catch {}
    }
    fetchCityByIP().then(cityName => {
      if (cityName) { const city = findCity(cityName); selectedCity.value = city; saveCity(city) }
    })
  }
}
loadCity()

export const useCity = () => ({
  selectedCity, isOpen,
  selectCity: (city: City) => { selectedCity.value = city; saveCity(city); isOpen.value = false },
  toggleDropdown: () => { isOpen.value = !isOpen.value },
  closeDropdown: () => { isOpen.value = false }
})
```

---

// Файл composables/useCart.ts — composable для корзины
```ts
import { ref, computed, watch, onMounted } from 'vue'
import { useAuth } from '~/composables/useAuth'

export interface CartItem {
  id: number; name: string; article: string; price: number
  quantity: number; image: string; colorName?: string
  colorClass?: string; originalPrice?: number; collectionSlug?: string
}

const cart = ref<CartItem[]>([])
const notificationText = ref('')
const notificationVisible = ref(false)
const notificationType = ref<'success' | 'warning'>('success')
let notificationTimer: ReturnType<typeof setTimeout> | null = null

function showAddNotification(name: string) {
  if (notificationTimer) clearTimeout(notificationTimer)
  notificationText.value = `«${name}» добавлен в корзину`
  notificationType.value = 'success'
  notificationVisible.value = true
  notificationTimer = setTimeout(() => { notificationVisible.value = false }, 2500)
}

function showWarning(text: string) {
  if (notificationTimer) clearTimeout(notificationTimer)
  notificationText.value = text
  notificationType.value = 'warning'
  notificationVisible.value = true
  notificationTimer = setTimeout(() => { notificationVisible.value = false }, 3000)
}

export const useCart = () => {
  const loadCart = () => {
    if (process.client) {
      try {
        const saved = localStorage.getItem('clickwood_cart')
        if (saved) cart.value = JSON.parse(saved)
      } catch { cart.value = [] }
    }
  }
  const saveCart = () => {
    if (process.client) localStorage.setItem('clickwood_cart', JSON.stringify(cart.value))
  }

  const addItem = (item: Omit<CartItem, 'quantity'>) => {
    const existing = cart.value.find(
      i => i.id === item.id && i.colorName === item.colorName && i.collectionSlug === item.collectionSlug
    )
    if (existing) existing.quantity += 1
    else cart.value.push({ ...item, quantity: 1 })
    saveCart()
    showAddNotification(item.name)
  }

  const totalItems = computed(() => cart.value.reduce((s, i) => s + i.quantity, 0))
  const totalPrice = computed(() => cart.value.reduce((s, i) => s + i.price * i.quantity, 0))

  onMounted(loadCart)
  watch(cart, saveCart, { deep: true })

  return {
    cart, addItem, totalItems, totalPrice,
    notificationText, notificationVisible, notificationType, showWarning
  }
}
```

---

// Файл pages/index.vue — главная страница (ключевые секции)
```vue
<script setup lang="ts">
import { ref, onMounted } from 'vue'
const router = useRouter()

// Слайдеры Swiper
const collectionSwiper = ref<any>(null)
const worksSwiper = ref<any>(null)
const sliderProducts = ref<any[]>([])

onMounted(async () => {
  sliderProducts.value = await $fetch('/api/slider')
  // Инициализация Swiper
  const Swiper = (await import('swiper')).default
  const { Navigation, Mousewheel, Keyboard } = await import('swiper/modules')
  collectionSwiper.value = new Swiper('.collection-slider', {
    slidesPerView: 1, spaceBetween: 24, loop: true, speed: 600,
    breakpoints: { 768: { slidesPerView: 3 } }
  })
  worksSwiper.value = new Swiper('.works-slider', {
    slidesPerView: 1, spaceBetween: 24, loop: true, speed: 600,
    breakpoints: { 768: { slidesPerView: 3 } }
  })
})
</script>

<template>
  <main>
    <!-- Hero секция -->
    <section class="h-[724px] bg-gray-200 flex items-center justify-center relative">
      <div class="container mx-auto px-4 text-center z-10">
        <h1 class="font-heading text-white text-5xl md:text-6xl">Создайте интерьер своей мечты вместе с нами!</h1>
        <p class="font-body text-white text-xl md:text-2xl mt-6">У нас вы найдете стильную и надежную мебель</p>
      </div>
    </section>

    <!-- Секция подписки -->
    <section class="bg-primary mt-20 pt-10 pb-12 relative">
      <img src="/images/chair.png" alt="Стул" class="hidden md:block absolute left-8 -top-10 h-[350px]" />
      <div class="container mx-auto px-4">
        <h2 class="text-white text-center mb-10">Дополнительная скидка до 5 000 ₽ за подписку на рассылку</h2>
        <div class="flex flex-col md:flex-row items-center justify-center gap-6">
          <input type="email" placeholder="EMAIL" class="px-8 py-4 rounded-[24px] w-full md:w-[480px]" />
          <button class="bg-[#5D4433] text-white px-12 py-4 rounded-[24px]">Подписаться</button>
        </div>
      </div>
    </section>

    <!-- Новая коллекция (слайдер) -->
    <section class="py-16">
      <div class="container mx-auto px-4">
        <div class="collection-slider swiper">
          <div class="swiper-wrapper">
            <div v-for="product in sliderProducts" :key="product.id" class="swiper-slide">
              <!-- Карточка товара -->
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- О нас -->
    <section class="py-16">
      <div class="container mx-auto px-4">
        <h2 class="text-textMain text-5xl text-center mb-12">О нас</h2>
        <p>CLICKWOOD – это пространство, где рождаются идеи для вашего интерьера.</p>
      </div>
    </section>

    <!-- Наши работы (слайдер) -->
    <section class="py-16">
      <div class="container mx-auto px-4">
        <div class="works-slider swiper">
          <div class="swiper-wrapper">
            <!-- Слайды с изображениями проектов -->
          </div>
        </div>
      </div>
    </section>
  </main>
</template>
```

---

// Файл pages/catalog.vue — каталог товаров (фильтры и сетка)
```vue
<script setup>
import { ref, computed, onMounted } from 'vue'
import { useCart } from '~/composables/useCart'
import { useProducts } from '~/composables/useProducts'

const { addItem } = useCart()
const { products, fetchProducts } = useProducts()

const categories = [
  { id: 'all', name: 'все товары' }, { id: 'sofas', name: 'диваны' },
  { id: 'chairs', name: 'кресла' }, { id: 'storage', name: 'хранение' },
  { id: 'tables', name: 'столы' }, { id: 'decor', name: 'декор' },
  { id: 'lighting', name: 'освещение' }
]

const styles = [
  { id: 'all', name: 'любой' }, { id: 'scandi', name: 'сканди' },
  { id: 'minimal', name: 'минимализм' }, { id: 'boho', name: 'бохо' },
  { id: 'artdeco', name: 'ар деко' }
]

// Состояние фильтров
const activeCategoryId = ref('all')
const activeStyleId = ref('all')
const activeColorHex = ref('')
const activePriceMin = ref(0)
const activePriceMax = ref(150000)
const onlyInStock = ref(false)
const onlyOnSale = ref(false)
const sortBy = ref('default')
const gridColumns = ref(3)

const activeDropdown = ref(null)
const isMobileFiltersOpen = ref(false)

const filteredProducts = computed(() => {
  let result = products.value
  if (activeCategoryId.value !== 'all') result = result.filter(p => p.categoryId === activeCategoryId.value)
  if (activeStyleId.value !== 'all') result = result.filter(p => p.styleId === activeStyleId.value)
  if (activeColorHex.value) result = result.filter(p => p.colorVariants?.some(v => v.color === activeColorHex.value))
  result = result.filter(p => p.price >= activePriceMin.value && p.price <= activePriceMax.value)
  if (onlyInStock.value) result = result.filter(p => p.inStock)
  if (onlyOnSale.value) result = result.filter(p => p.discount > 0)
  // Сортировка
  return result
})

// Методы для выбора фильтров
const selectCategory = (id) => { activeCategoryId.value = id; activeDropdown.value = null }
const toggleMobileFilters = () => { isMobileFiltersOpen.value = !isMobileFiltersOpen.value }
const resetAllFilters = () => { /* сброс всех фильтров */ }

onMounted(fetchProducts)
</script>

<template>
  <main>
    <!-- Верхняя панель с категориями -->
    <section class="bg-primary py-3">
      <nav class="container mx-auto px-4 flex gap-8">
        <button v-for="cat in categories" :key="cat.id" @click="selectCategory(cat.id)">{{ cat.name }}</button>
      </nav>
    </section>

    <!-- Панель фильтров -->
    <section class="py-6 border-b border-border">
      <div class="container mx-auto px-4">
        <!-- Десктопная панель (скрыта на lg-) -->
        <div class="hidden lg:flex items-center justify-between gap-4">
          <div class="flex items-center gap-3 flex-wrap">
            <!-- Выпадающие списки: категория, цена, цвет, стиль, наличие, скидка -->
          </div>
          <!-- Сортировка и переключение сетки 3/4 колонки -->
        </div>

        <!-- Мобильная панель (видна на lg-) -->
        <div class="flex lg:hidden items-center justify-between">
          <button @click="toggleMobileFilters">Фильтры</button>
          <select v-model="sortBy"><option v-for="opt in sortOptions">{{ opt.name }}</option></select>
        </div>

        <!-- Мобильная панель фильтров (выдвижная) -->
        <Transition><div v-if="isMobileFiltersOpen" class="lg:hidden mt-4 p-4 border rounded-lg bg-white">
          <!-- select: категория, цена, цвет, стиль, чекбоксы -->
        </div></Transition>
      </div>
    </section>

    <!-- Сетка товаров -->
    <section class="py-10">
      <div class="container mx-auto px-4">
        <div class="grid gap-x-[60px] gap-y-12"
          :class="gridColumns === 3 ? 'grid-cols-1 md:grid-cols-3' : 'grid-cols-1 md:grid-cols-2 lg:grid-cols-4'">
          <NuxtLink v-for="product in filteredProducts" :key="product.id" :to="`/product/${product.id}`">
            <NuxtImg :src="product.image" :alt="product.name" class="w-full h-[320px] object-cover" />
            <h3>{{ product.name }}</h3>
            <p>{{ product.price.toLocaleString('ru-RU') }} ₽</p>
          </NuxtLink>
        </div>
      </div>
    </section>
  </main>
</template>
```

---

// Файл pages/product/[id].vue — карточка товара
```vue
<script setup lang="ts">
import { ref, computed } from 'vue'
import { useRoute, useRouter } from 'vue-router'
import { useCart } from '~/composables/useCart'
import { useProducts } from '~/composables/useProducts'
import type { ProductData } from '~/server/types/product'

const { addItem, showWarning } = useCart()
const { fetchProduct } = useProducts()
const route = useRoute()
const router = useRouter()

const productIdNum = Number(route.params.id)
const { data: product } = await useAsyncData(`product-${productIdNum}`, () => fetchProduct(productIdNum))

const activeImage = ref(0)
const selectedColor = ref<number | null>(null)
const activeTab = ref('description')
const showInstallmentInfo = ref(false)
const productColors = computed(() => product.value?.colorVariants || [])

const currentVariantImages = computed(() => {
  const base = product.value?.images || [product.value?.image].filter(Boolean) as string[]
  if (selectedColor.value === null) return base
  const variant = productColors.value[selectedColor.value]
  return variant?.images?.length ? [...variant.images, ...base] : base
})

const addToCart = () => {
  if (!product.value) return
  if (productColors.value.length && selectedColor.value === null) {
    showWarning('Пожалуйста, выберите цвет')
    return
  }
  addItem({ id: product.value.id, name: product.value.name, price: product.value.price, image: product.value.image, article: product.value.article })
}

// Свайп изображений
const touchStartX = ref(0)
const nextImage = () => { /* переход к следующему */ }
const prevImage = () => { /* переход к предыдущему */ }
</script>

<template>
  <main v-if="product">
    <!-- Хлебные крошки -->
    <section class="py-4 bg-gray-50">
      <div class="container mx-auto px-4 flex gap-2 text-sm">
        <NuxtLink to="/">Главная</NuxtLink><span>/</span>
        <NuxtLink to="/catalog">Каталог</NuxtLink><span>/</span>
        <span>{{ product.name }}</span>
      </div>
    </section>

    <section class="py-12">
      <div class="container mx-auto px-4 grid grid-cols-1 lg:grid-cols-2 gap-12">

        <!-- Колонка изображений -->
        <div class="flex gap-4">
          <!-- Миниатюры (только десктоп) -->
          <div class="hidden md:flex flex-col gap-3">
            <div v-for="(image, index) in currentVariantImages" :key="index" @click="activeImage = index"
              class="w-[120px] h-[120px] border-2 cursor-pointer"
              :class="{ 'border-primary': activeImage === index }">
              <NuxtImg :src="image" class="w-full h-full object-cover" />
            </div>
          </div>
          <!-- Главное изображение с кнопками и свайпом -->
          <div class="flex-1 relative select-none" @touchstart="onTouchStart" @touchend="onTouchEnd">
            <NuxtImg :src="currentVariantImages[activeImage]" class="w-full max-h-[500px] object-contain" />
            <button @click="prevImage" class="absolute left-2 top-1/2 w-12 h-12 bg-black/20 rounded-full">‹</button>
            <button @click="nextImage" class="absolute right-2 top-1/2 w-12 h-12 bg-black/20 rounded-full">›</button>
          </div>
        </div>
        <!-- Индикатор прогресса (мобильные) -->
        <div v-if="currentVariantImages.length > 1" class="flex justify-center gap-1.5 mt-3 md:hidden">
          <div v-for="(_, index) in currentVariantImages" :key="index"
            class="w-2 h-2 rounded-full" :class="activeImage === index ? 'bg-primary' : 'bg-gray-300'"></div>
        </div>

        <!-- Информация о товаре -->
        <div>
          <h1 class="font-heading text-4xl mb-2">{{ product.name }}</h1>
          <div class="text-gray-500 mb-4">Код товара: {{ product.article }}</div>

          <!-- Цвета -->
          <div v-if="productColors.length > 0" class="mb-6">
            <p class="text-sm mb-2">Цвет:</p>
            <div class="flex items-center gap-3">
              <div v-for="(color, index) in productColors" :key="index" @click="selectColor(index)"
                class="w-6 h-6 rounded-full cursor-pointer border-2 border-white shadow"
                :class="selectedColor === index ? 'ring-2 ring-primary' : ''"
                :style="{ backgroundColor: color.color }" :title="color.name"></div>
              <span v-if="selectedColor !== null" class="text-sm">{{ productColors[selectedColor]?.name }}</span>
            </div>
          </div>

          <!-- Цена -->
          <div class="font-body text-4xl text-textMain mb-6">
            {{ product.price.toLocaleString('ru-RU') }} ₽
          </div>

          <!-- Кнопки -->
          <button @click="addToCart" class="w-full py-3 bg-primary text-white rounded-full">В корзину</button>
          <button @click="buyNow" class="w-full py-3 border border-primary text-primary rounded-full mt-3">Купить в 1 клик</button>

          <!-- Дополнительная информация -->
          <div class="mt-6 space-y-4">
            <div @click="navigateTo('/delivery')" class="flex justify-between py-3 border-b cursor-pointer">
              <span>Стоимость доставки</span><span>›</span>
            </div>
            <div @click="toggleInstallmentInfo" class="flex justify-between py-3 border-b cursor-pointer">
              <span>Рассрочка без переплаты</span>
              <svg :class="{ 'rotate-90': showInstallmentInfo }"><path d="M1 1L5 5L9 1"/></svg>
            </div>
            <Transition><div v-if="showInstallmentInfo" class="py-3 text-sm">
              Рассрочка без переплаты на срок от 3 до 24 месяцев.
            </div></Transition>
          </div>
        </div>
      </div>

      <!-- Табы: описание, характеристики, отзывы, вопросы -->
      <div class="mt-16">
        <div class="flex gap-4 md:gap-12 overflow-x-auto whitespace-nowrap border-b scrollbar-hide">
          <button v-for="tab in ['description', 'characteristics', 'reviews', 'questions']" :key="tab"
            @click="activeTab = tab"
            class="pb-4 font-body text-sm md:text-lg flex-shrink-0"
            :class="activeTab === tab ? 'border-b-2 border-primary text-primary' : 'text-gray-500'">
            {{ tab === 'description' ? 'Описание' : tab === 'characteristics' ? 'Характеристики' : tab }}
          </button>
        </div>
        <div class="max-w-3xl mt-8">{{ product.description }}</div>
      </div>
    </section>
  </main>
</template>

<style scoped>
.scrollbar-hide::-webkit-scrollbar { display: none; }
.scrollbar-hide { -ms-overflow-style: none; scrollbar-width: none; }
</style>